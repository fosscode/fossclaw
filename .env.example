# Example environment variables for FossClaw
PORT=3456
FOSSCLAW_CWD=/path/to/default/working/directory
FOSSCLAW_SESSION_DIR=~/.fossclaw/sessions

# Authentication (optional - auto-generates if not set)
# If not provided, random credentials are generated and saved to ~/.fossclaw/credentials.json
# FOSSCLAW_USER=admin
# FOSSCLAW_PASS=your_secure_password

# HTTPS configuration (optional - uses self-signed certs by default)
# HTTPS is mandatory and cannot be disabled
# FOSSCLAW_HTTPS_HOSTNAME=localhost
# FOSSCLAW_CERT_DIR=~/.fossclaw/certs

# Optional: Linear integration
# LINEAR_API_KEY=your_linear_api_key

# Optional: OpenCode configuration
# OPENCODE_PORT=3556

# Optional: Ollama auto-naming
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2:3b
